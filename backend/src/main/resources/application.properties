spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://db:3306/chattingo_db?createDatabaseIfNotExist=true}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:test@123}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.endpoint.health.probes.enabled=true

# JWT secret (fallback for local dev; override via env: JWT_SECRET)
jwt.secret=${JWT_SECRET:htxAPEWvHnRrMlL403iAktlmOFlseDUiPIqsECSQpAM=}

# CORS Configuration (can be overridden by environment variables)
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://13.222.158.219:3000,http://localhost}
cors.allowed.methods=${CORS_ALLOWED_METHODS:GET,POST,PUT,DELETE,OPTIONS}
